#Range() is a function returns a sequence of numbers, 
    #starting from 0 by default, and increments by 1 (by default), 
    #and stops before a specified number.
#"in", this keyword check if a value is present in sequence
    #also used to iterate through a sequence in for loop

for x in range (0, 11):
    print(x)